const registeredScreens = {
  ReplySelector: 'replies.replySelector.screen',
  QuickReplies: 'replies.quickReplies.screen',
  RepliesEditor: 'relies.repliesEditor.screen',
  AddReplyScreen: 'replies.addReplyScreen.screen'
};

module.exports = {
  registeredScreens
};
